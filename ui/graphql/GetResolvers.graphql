query GetResolvers($schemaId: Int!, $type: String!) {
  resolvers(schemaId: $schemaId, type: $type) {
    id
    type
    field
    responseMapping
    requestMapping
    DataSource {
        id
        name
        type
    }
  }
}