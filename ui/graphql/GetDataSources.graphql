query GetDataSources ($name: String) {
    dataSources(name: $name) {
        id
        name
        type
        config
        resolvers {
            id
            GraphQLSchemaId
            type
        }
    }
}